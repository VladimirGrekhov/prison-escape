# Maps the Upgrade request header so proxied locations can hand off WebSocket
# connections (used by the Prison Escape Colyseus multiplayer server).
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
